{
  "$comment": "Replace REPLACE-WITH-BACKEND-HOST with the public URL of the FastAPI backend (e.g. https://your-app.onrender.com) once it's deployed, then push. Vercel proxies /api/* there, same as the Vite dev proxy.",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://REPLACE-WITH-BACKEND-HOST/api/:path*" }
  ]
}
